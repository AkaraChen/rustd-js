import api from './index.js';
export const {
  versionCompare,
  versionIsValid,
  versionLang,
  TOKEN,
  SCAN_MODE,
  tokenLookup,
  tokenIsKeyword,
  tokenIsExported,
  tokenString,
  FileSet,
  GoFile,
  Scanner,
  GoScanError,
} = api;
