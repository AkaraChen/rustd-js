import api from './index.js';
export const {
  versionCompare,
  versionIsValid,
  versionLang,
  GoConstValue,
  constMakeInt64,
  constMakeBool,
  constMakeFromLiteral,
  constToFloat,
  constToComplex,
  constToInt,
  constToString,
  constBoolVal,
  constFloat64Val,
  constCompare,
  constCompareOp,
  constSign,
  constReal,
  constImag,
  constBitLen,
  constBinaryOp,
  constUnaryOp,
  constShift,
  TOKEN,
  SCAN_MODE,
  PARSE_MODE,
  tokenLookup,
  tokenIsKeyword,
  tokenIsExported,
  tokenString,
  FileSet,
  GoFile,
  Scanner,
  GoScanError,
  GoParseError,
  UnsupportedFeatureError,
  parseFile,
  parseExpr,
  astFprint,
  astInspect,
  astIsExported,
  astNewIdent,
} = api;
